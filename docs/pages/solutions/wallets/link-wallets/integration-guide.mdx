---
title: Sequence Wallet Linking Integration - Embedded Wallet Authentication & External Wallet Sign-in
description: This guide demonstrates how to integrate an Embedded Wallet with traditional authentication methods. It covers creating a verifiable link to sign into an external wallet, storing the session securely, and verifying the authentication.
---

import { Callout } from "vocs/components";

# Wallet Linking Integration Guide

In this guide, we will showcase how to sign into an [Embedded Wallet](/solutions/wallets/embedded-wallet/overview) using traditional web2 authentication like email or social login. Then, in order to link an external wallet address using [Sequence Kit](/solutions/wallets/sequence-kit/overview) to your parent Embedded Wallet, sign a message that will always be required which creates the generated linking of addresses for a single user profile stored off-chain privately. Finally, other features will be delved into, like listing total wallets linked and removal of the state of linked wallets, both using signatures for privacy preserving means.

1. [Sequence Builder Console Signup & Project Creation](/solutions/wallets/link-wallets/integration-guide#1-sequence-builder-console-signup--project-creation)
2. [Access Key Management](/solutions/wallets/link-wallets/integration-guide#2-access-key-management)
3. [Integrate an Embedded Wallet](/solutions/wallets/link-wallets/integration-guide#3-integrate-an-embedded-wallet)
4. [Sign In with Sequence Kit](/solutions/wallets/link-wallets/integration-guide#4-sign-in-with-sequence-kit)
5. [Integrate API Interface File](/solutions/wallets/link-wallets/integration-guide#5-integrate-api-interface-file)
6. [Link Sequence Kit Signed in Wallet](/solutions/wallets/link-wallets/integration-guide#6-link-sequence-kit-signed-in-wallet)
7. [Retrieve Linked Wallets](/solutions/wallets/link-wallets/integration-guide#7-retrieve-linked-wallets)
8. [Remove Linked Wallet](/solutions/wallets/link-wallets/integration-guide#8-remove-linked-wallet)

<Callout>
Give a [try of an example demo](https://demo-waas-wallet-link.pages.dev/)
</Callout>

## 1. Sequence Builder Console Signup & Project Creation

First start by following [this walkthrough](/support/builder/project-management) for how to sign up to the [Sequence Builder](https://sequence.build/) and to learn how to create a project.

## 2. Access Key Management

Now that you have a project, you'll need to obtain 2 different Access Keys for your project in order to authenticate your application with the Sequence Stack:
1. `Waas Config Key` used for the Embedded Wallet, which can be learned about [here](/solutions/builder/embedded-wallet).
2. `Project Access Key` used for the Embedded Wallet, Sequence Kit, & Sequence Indexer, which can be done [here](/solutions/builder/getting-started#claim-an-api-access-key).

## 3. Integrate an Embedded Wallet

Quickstart yourself with an Embedded Wallet React based application using the Sequence CLI, using the following command in your terminal:

```shell
npx sequence-cli boilerplates create-embedded-wallet-react-starter
```

This command inputted in your terminal will present options to input the `Waas Config Key` and `Access Keys` you received in [step 2](/solutions/wallets/link-wallets/integration-guide#2-access-key-management), after, you'll need to set up your `Client IDs` (if using social) for the Login Providers.

Choose your way to authenticate a user from the various Login provider types: [Email](/solutions/wallets/embedded-wallet/examples/authentication#email-authentication), [Google](/solutions/builder/embedded-wallet/google-configuration/), [Apple](/solutions/builder/embedded-wallet/apple-configuration/), or [PlayFab](/solutions/builder/embedded-wallet/playfab-configuration/).

## 4. Sign In with Sequence Kit

Next, follow the [following quickstart guide](/solutions/wallets/sequence-kit/getting-started#setting-up-your-dapp) in order to create a way to sign in a linked wallet using Sequence Kit, and ensure that you can read a wallet address using a React hook.

## 5. Integrate API Interface File

In order to begin using the API, an interface file will need to be integrated manually into your application, so that the interface in the file can be instantiated and called upon.

The [source code for the interface can be found here](https://github.com/0xsequence-demos/demo-waas-wallet-link/blob/ddfba3f59ee97e9bc2b67bc751db3fb2becc5946/src/api/api.gen.ts) which then is instantiated with the existing Sequence server like such:

```typescript
import { 
    API,            // API interface
    LinkedWallet    // typed class
} from "./api.gen";

const api = new API("https://dev-api.sequence.app", fetch);
```

### API Functions Used for this Guide
1. [`linkWallet(...)`](/solutions/wallets/link-wallets/integration-guide#6-link-sequence-kit-signed-in-wallet)
2. [`getLinkedWallets(...)`](/solutions/wallets/link-wallets/integration-guide#7-retrieve-linked-wallets)
3. [`removeLinkedWallet(...)`](/solutions/wallets/link-wallets/integration-guide#8-remove-linked-wallet)

## 6. Link Sequence Kit Signed in Wallet

In order to link a Sequence Kit signed in wallet (e.g. EOA, Universal Wallet, etc.) to an Embedded Wallet, begin with the following API function `linkWallet`, the typed arguments and return types (negating the optional values):

```typescript
interface LinkWalletArgs {
  parentWalletAddress: string
  parentWalletMessage: string
  parentWalletSignature: string
  linkedWalletAddress: string
  linkedWalletMessage: string
  linkedWalletSignature: string
  signatureChainId: string
  linkedWalletType?: string
}

interface LinkWalletReturn {
  status: boolean
}

linkWallet(args: LinkWalletArgs, headers?: object, signal?: AbortSignal): Promise<LinkWalletReturn>
```

This can be implemented in the following way, with an integration with [wagmi](https://wagmi.sh/) for the Sequence Kit message signing:
::::steps

### Sign Message to Attest to Owning an Address

There are two signatures required: one for the Embedded Wallet signing the Sequence Kit wallet address (linked wallet), and the linked wallet (from Sequence Kit) signing the parent wallet address (Embedded Wallet). You can sign any message presented to the user, as long as the message and signature are consistent with what is passed to the API later:

```typescript
import {
  ...
  useSignMessage
  ...
} from "wagmi";

function App() {

    const { signMessageAsync } = useSignMessage();
    const { address: kitWalletAddress } = useAccount();

    const getSignaturesForLinking = () => {
        const parentWalletMessage = "linked wallet with address "
        const linkedWalletMessage = "parent wallet with address "
        const parentMessage = parentWalletMessage + kitWalletAddress
        const parentWalletAddress = await sequenceWaas.getAddress()
        const linkingMessage = "Link to " + linkedWalletMessage + parentWalletAddress

        const response = await signMessageAsync({
            message: linkingMessage,
        })

        const parentSignatureRes = await sequenceWaas.signMessage({
            message: parentMessage
        })

        const parentSignature = parentSignatureRes.data.signature
        const linkedSignature = response.result
        const linkedWalletAddress = kitWalletAddress

        return { parentWalletAddress, linkedWalletAddress, parentMessage, linkingMessage, parentSignature, linkedSignature }
    }
}
```

### Use API to Add Signature Payload & Parent Wallet Address
Call the previous `getSignaturesForLinking` function and unpack the response to pass the responses into the `api.linkWallet` function:

```typescript
function App(){
    ...
    const linkWallet = () => {
        const signaturesResult = await getSignaturesForLinking()
        const { parentWalletAddress, linkedWalletAddress, parentMessage, linkingMessage, parentSignature, linkedSignature } = signatureResult

        const response = await api.linkWallet({
            signatureChainId: "137", // network chosen for both embedded wallet and sequence kit
            parentWalletAddress,
            parentWalletMessage: parentMessage,
            parentWalletSignature: parentSignature,
            linkedWalletAddress: linkedWalletAddress,
            linkedWalletMessage: linkingMessage,
            linkedWalletSignature: linkedSignature,
        })

        console.log(`status: ${response.status}`)
    }
}
```

::::

## 7. Retrieve Linked Wallets

To protect the privacy of users, you can pass in a signature generated from the parent Embedded Wallet in order to retrieve the list of wallet addresses that is linked to a parent wallet address.

The API will return the linked wallet objects in the following format:

```typescript
interface LinkedWallet {
  id: number
  walletType?: string
  walletAddress: string
  linkedWalletAddress: string
  createdAt?: string
}

interface GetLinkedWalletsReturn {
  linkedWallets: Array<LinkedWallet>
}
```

And this can be accomplished in the following way, by passing in a message, getting a signature from the parent Embedded Wallet, checking to see the signature is readable, then passing the variables: `parentWalletAddress`,`parentWalletMessage`,`parentWalletSignature` and `signatureChainId` to the API:

```typescript
const getLinkedWallets = () => {
    const message = "parent wallet with address " + parentWalletAddress
    const signature = await sequenceWaas.signMessage({
        message: message,
    })

    if (!signature.data.signature) {
        console.error("Could not get signature from wallet to be linked")
        throw new Error("Could not get signature from wallet to be linked")
    }

    const response = await api.getLinkedWallets({
        parentWalletAddress: parentWalletAddress as `0x${string}`,
        parentWalletMessage: message,
        parentWalletSignature: signature.data.signature,
        signatureChainId: "137",
    })

    response.linkedWallets.map((linkedWallet: LinkedWallet) => console.log(linkedWallet))

    return response.linkedWallets
}
```

## 8. Remove Linked Wallet
Finally, if you no longer want to keep a wallet linked off-chain to a parent wallet, you can perform a signature by both wallet instances (Embedded Wallet & Sequence Kit linked wallet), and pass in the linked wallet address, [like in step 6](/solutions/wallets/link-wallets/integration-guide#6-link-sequence-kit-signed-in-wallet) in order to remove the linked state:

::::steps
### Sign Message to Attest to Removing a Linked Address

```typescript
import {
  ...
  useSignMessage
  ...
} from "wagmi";

function App() {

    const { address: kitWalletAddress } = useAccount();
    const { signMessageAsync } = useSignMessage();

    const getSignaturesForUnlinking = () => {
        const linkedWalletMessage = "parent wallet with address "
        const parentWalletAddress = await sequenceWaas.getAddress()
        const linkingMessage = "Unlink from " + linkedWalletMessage + parentWalletAddress

        const response = await signMessageAsync({
            message: linkingMessage,
        })

        const parentSignatureRes = await sequenceWaas.signMessage({
            message: parentMessage
        })

        const parentSignature = parentSignatureRes.data.signature
        const linkedSignature = response.result
        const linkedWalletAddress = kitWalletAddress

        return { parentWalletAddress, linkedWalletAddress, parentMessage, linkingMessage, parentSignature, linkedSignature }
    }
}
```

### Use API to Add Signature Payload for Parent Address
```typescript
function App(){
    ...
    const linkWallet = () => {
        const signaturesResult = await getSignaturesForUnlinking()
        const { parentWalletAddress, linkedWalletAddress, parentMessage, linkingMessage, parentSignature, linkedSignature } = signatureResult;

        const response = await api.removeLinkedWallet({
            signatureChainId: "137", // network chosen for both Embedded Wallet and Sequence Kit
            parentWalletAddress,
            parentWalletMessage: parentMessage,
            parentWalletSignature: parentSignature,
            linkedWalletAddress: linkedWalletAddress,
            linkedWalletMessage: linkingMessage,
            linkedWalletSignature: linkedSignature,
        });

        console.log(`status: ${response.status}`)
    }
}
```
::::

## Conclusion

We've successfully cloned down from github a boilerplate repository and setup a Login Provider to connect an Embedded Wallet with the application. Then, we enabled the signing in of a Sequence Kit external wallet. Finally, we signed messages and created an authenticated way to add addresses to a parent wallet address in order to link a users' provably owned wallets together in a user friendly signature interface.