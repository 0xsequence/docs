---
ja-translated title: SequenceEthereum Client Methods and Usage
description: Use SequenceEthereum to create a Client and utilize our Node Gateway service for RPC requests.
---

# MOCKDATA + ja-translated Clients
ja-translated In the context of SequenceEthereum, a `Client` is defined by the [`IEthClient` interface](https://github.com/0xsequence/sequence-unity/blob/master/Assets/SequenceSDK/Ethereum/Provider/IEthClient.cs).

MOCKDATA + ja-translated Creating a client requires a `Chain`. This will use our highly available and responsive Node Gateway service for your RPC requests; accessed using the API key from the Builder you've provided in your `SequenceConfig` scriptable object. If you prefer to use your own RPC URL, you can create a `SequenceEthClient` using a URL string as a parameter instead.

ja-translated You can create a client using this snippet:

```csharp
IEthClient client = new SequenceEthClient(Chain.Polygon);
```

## ja-translated Methods
MOCKDATA + ja-translated As your connection point to Ethereum nodes, there are a number of methods that can be performed by a client, these can be found in the `IEthClient` interface and are implemented by `SequenceEthClient`.

MOCKDATA + ja-translated Note: with the exception of BalanceAt (potentially), most users will not need to make use of these methods, but we have included them in our documentation for completeness.

### ja-translated BalanceAt
MOCKDATA + ja-translated Used to get the gas currency balance of a given wallet at a given blockNumber (in hexadecimal format provided as a string)

```csharp
BigIntegar balance = await client.BalanceAt(wallet.GetAddress()); // By default, if no blockNumber string is provided, check the latest block
BigIntegar balance = await client.BalanceAt(wallet.GetAddress(), blockNumber);
```

ja-translated Note: there are two special values for blockNumber. "earliest" will get the balance at the earliest block on the chain. "latest" will get the balance at the latest block on the chain and is the default parameter when none is provided. Otherwise, you'll want to provide the blockNumber string in hexadecimal format.

MOCKDATA + ja-translated :::warning
<b><u>Unless you <i>really</i> want to get into the weeds with how things work, it is HIGHLY recommended to move on to the next page of the documentation at this point</u></b>
:::

### ja-translated BlockByNumber
MOCKDATA + ja-translated Used to get the `Block` with a specific block number.

```csharp
Block block = await client.BlockByNumber(blockNumber);
```

ja-translated Note: as above, blockNumber should be in hexadecimal format or special values "ealiest" and "latest"

### MOCKDATA + ja-translated BlockByHash
ja-translated Used to get the `Block` by a specified block hash (string)

```csharp
Block block = await client.BlockByHash(blockHash);
```

### MOCKDATA + ja-translated BlockNumber
ja-translated Used to get the most recent block number in hexadecimal format

```csharp
string blockNumber = await client.BlockNumber();
```

### MOCKDATA + ja-translated BlockRange
ja-translated Used to get a `List<Block>` from the blocks in a range specified by blockNumbers

```csharp
List<Block> blockRange = await client.BlockRange(startingBlockNumber, endingBlockNumber);
```

ja-translated Note: as above, blockNumber should be in hexadecimal format or special values "ealiest" and "latest"

### MOCKDATA + ja-translated ChainID
ja-translated Used to get the chain id in hexadecimal format for the chain the client is connected to

```csharp
string chainId = await client.ChainID();
```

### MOCKDATA + ja-translated CodeAt
ja-translated Used to get the bytecode for a smart contract at a given address in hexadecimal format at a specified blockNumber

```csharp
string code = await client.CodeAt(contractAddress, blockNumber);
```

ja-translated Note: as above, blockNumber should be in hexadecimal format or special values "ealiest" and "latest"

### MOCKDATA + ja-translated EstimateGas
ja-translated Given a `TransactionCall` estimate the amount of gas required for the transaction

```csharp
BigIntegar gas = await client.EstimateGas(transactionCall);
```

### MOCKDATA + ja-translated FeeHistory
ja-translated Get a `FeeHistoryResult` for gas fees paid blockCount blocks since newestBlock (blockNumber)

```csharp
FeeHistoryResult feeHistory = await client.FeeHistory(blockCount, newestBlock, new int[] { });
```

ja-translated Note: as above, blockNumber should be in hexadecimal format or special values "ealiest" and "latest"

### MOCKDATA + ja-translated NetworkId
ja-translated Used to get the chain id in integer format (as string) for the chain the client is connected to

```csharp
string networkId = await client.NetworkId();
```

### MOCKDATA + ja-translated NonceAt
ja-translated Used to get the recommended nonce for a given `Address` at a given blockNumber (defaults to "latest")

```csharp
BigInteger nonce = await client.NonceAt(wallet.GetAddress()); // Nonce at latest
BigIntegar nonce = await client.NonceAt(wallet.GetAddress(), blockNumber);
```

ja-translated Note: as above, blockNumber should be in hexadecimal format or special values "ealiest" and "latest"

### MOCKDATA + ja-translated SendRawTransaction
ja-translated Given a signed transaction string, submit the transaction to the network and return a transaction hash

```csharp
string transactionHash = await client.SendRawTransaction(signedTransactionString);
```

### MOCKDATA + ja-translated SuggestGasPrice
ja-translated Used to get a suggested gas price

```csharp
BigIntegar gasPrice = await client.SuggestGasPrice();
```

### MOCKDATA + ja-translated SuggestGasTipCap
ja-translated Used to get the max suggested priority fee for gas

```csharp
BigIntegar gasTipCap = await client.SuggestGasTipCap();
```

### MOCKDATA + ja-translated TransactionByHash
ja-translated Used to get a `Transaction` by transaction hash

```csharp
Transaction transaction = await client.TransactionByHash(transactionHash);
```

### MOCKDATA + ja-translated TransactionCount
ja-translated Used to get the number of transactions in a block by block hash

```csharp
BigIntegar transactionCount = await client.TransactionCount(blockHash);
```

### MOCKDATA + ja-translated WaitForTransactionReceipt
ja-translated Provide a transaction hash in order to wait for and return the `TransactionReceipt`

```csharp
TransactionReceipt receipt = await client.WaitForTransactionReceipt(transactionHash);
```