---
title: MOCKDATA + ja-translated Transak Integration - Onboarding User Funds with Credit Card On-Ramp
description: ja-translated Allow your users to easily purchase cryptocurrencies with their credit card through the Transak integration in your game.
---

# ja-translated Onboard User Funds
MOCKDATA + ja-translated If you wish for your users to be able to pay for things in your game using cryptocurrencies, you'll find our Transak integration to be very helpful.
Transak allows users to purchase a variety of cryptocurrencies on a variety of blockchains using their credit card using a user friendly web interface. Learn more about Transak [here](https://transak.com/).

## ja-translated On-Ramp User Funds via Credit Card
MOCKDATA + ja-translated To generate a link for your user's Transak on-ramping:

```csharp
Transak onRamp = new TransakOnRamp(userWalletAddress);
string onRampLink = onRamp.GetTransakLink(); // You'll notice via IntelliSense that there are a number of default values you can provide to help tailor your UX
```

ja-translated From here, you can open the `onRampLink` via `Application.OpenURL` or similar. Or, you can also use:

```csharp
Transak onRamp = new TransakOnRamp(userWalletAddress);
onRamp.OpenTransakLink(); // You'll notice via IntelliSense that there are a number of default values you can provide to help tailor your UX
```

MOCKDATA + ja-translated This will automatically call `Application.OpenURL` on the generated Transak On-Ramp link.

## ja-translated Check Supported Countries
MOCKDATA + ja-translated To check the countries supported by Transak (and learn about how they are supported), use the `GetSupportedCountries` Task.

```csharp
try {
    SupportedCountry[] supportedCountries = await TransakOnRamp.GetSupportedCountries();
}
catch (Exception e) {
    Debug.LogError($"Encountered error fetching supported countries from Transak: {e.Message}");
}
```