---
ja-translated title: Customizing Sequence Kit with Configuration Options
description: The Configuration section allows developers to customize the Sequence Kit by passing options to the KitProvider wrapper.
sidebar_label: Configuration
---

# MOCKDATA + ja-translated Custom Configuration Options

ja-translated Developers can customize the Sequence Kit experience by passing configuration options to the `SequenceKit` wrapper.

MOCKDATA + ja-translated Here's how you can configure the kit using these options:

```jsx
  const kitConfig =  {
  defaultTheme: 'light',
  position: 'top-left',
  ...
 }

  <SequenceKit config={kitConfig}>
    <App />
  <SequenceKit>

```

## ja-translated Configuration Overview

MOCKDATA + ja-translated The following is the available configuration customization options, or, [see below for all the options in-depth](/solutions/wallets/sequence-kit/custom-configuration#available-options):

```typescript
interface CreateConfigOptions {
  appName: string;
  projectAccessKey: string;
  chainIds?: number[];
  defaultChainId?: number;
  disableAnalytics?: boolean;
  defaultTheme?: Theme;
  position?: ModalPosition;
  signIn?: {
    logoUrl?: string;
    projectName?: string;
    useMock?: boolean;
  };
  displayedAssets?: Array<{
    contractAddress: string;
    chainId: number;
  }>;
  ethAuth?: EthAuthSettings;
  isDev?: boolean;

  wagmiConfig?: WagmiConfig; // optional wagmiConfig overrides

  waasConfigKey?: string;
  enableConfirmationModal?: boolean;

  walletConnect?:
    | false
    | {
        projectId: string;
      };

  google?:
    | false
    | {
        clientId: string;
      };

  apple?:
    | false
    | {
        clientId: string;
        redirectURI: string;
      };

  email?:
    | boolean
    | {
        legacyEmailAuth?: boolean;
      };
}
```

## ja-translated Custom Styling and Design

MOCKDATA + ja-translated You can customize the visual appearance of Sequence Kit by providing a custom theme object to the `defaultTheme` configuration option. Here's an example of how to implement custom styling:

```typescript
import { createConfig } from "@0xsequence/kit";

const CUSTOM_THEME = {
  backgroundPrimary: "rgba(35, 100, 32, 1)",
  backgroundSecondary: "navy",
};

export const config = createConfig("waas", {
  projectAccessKey: projectAccessKey,
  defaultTheme: CUSTOM_THEME,
  // ... other config options
});
```

### ja-translated Available Theme Variables

MOCKDATA + ja-translated The following theme variables can be customized:

```typescript
// Text Colors
text100: string; // 'rgba(255, 255, 255, 1)'
text80: string; // 'rgba(255, 255, 255, 0.8)'
text50: string; // 'rgba(255, 255, 255, 0.5)'
textInverse100: string; // 'rgba(0, 0, 0, 1)'

// Background Colors
backgroundPrimary: string; // 'rgba(0, 0, 0, 1)'
backgroundSecondary: string; // 'rgba(255, 255, 255, 0.1)'
backgroundContrast: string; // 'rgba(0, 0, 0, 0.5)'
backgroundMuted: string; // 'rgba(255, 255, 255, 0.05)'
backgroundControl: string; // 'rgba(255, 255, 255, 0.25)'
backgroundInverse: string; // 'rgba(255, 255, 255, 1)'
backgroundBackdrop: string; // 'rgba(34, 34, 34, 0.9)'
backgroundOverlay: string; // 'rgba(0, 0, 0, 0.7)'
backgroundRaised: string; // 'rgba(54, 54, 54, 0.7)'

// Button Colors
buttonGlass: string; // 'rgba(255, 255, 255, 0.15)'
buttonEmphasis: string; // 'rgba(0, 0, 0, 0.5)'
buttonInverse: string; // 'rgba(255, 255, 255, 0.8)'

// Border Colors
borderNormal: string; // 'rgba(255, 255, 255, 0.25)'
borderFocus: string; // 'rgba(255, 255, 255, 0.5)'
```

ja-translated You can override any combination of these variables to create your custom theme. The values can be provided in any valid CSS color format (rgba, hex, named colors, etc.).

# MOCKDATA + ja-translated Available Options

## ja-translated Sequence App Development

### MOCKDATA + ja-translated `appName`

| MOCKDATA + ja-translated Type   | ja-translated Default              |
| ------------------------------- | ---------------------------------- |
| MOCKDATA + ja-translated string | MOCKDATA + ja-translated undefined |

ja-translated An internal software naming value that is not presented to the user.

### MOCKDATA + ja-translated `projectAccessKey`

| MOCKDATA + ja-translated Type   | ja-translated Default              |
| ------------------------------- | ---------------------------------- |
| MOCKDATA + ja-translated string | MOCKDATA + ja-translated undefined |

ja-translated The project access key that is required, obtained from [Sequence Builder](https://sequence.build)

### MOCKDATA + ja-translated `ethAuth` as EthAuthSettings

```typescript
{
  /*app name*/
  app?: string
  /** expiry number (in seconds) that is used for ETHAuth proof. Default is 1 week in seconds. */
  expiry?: number
  /** origin hint of the dapp's host opening the wallet. This value will automatically
   * be determined and verified for integrity, and can be omitted. */
  origin?: string
  /** nonce is an optional number to be passed as ETHAuth's nonce claim for replay protection. **/
  nonce?: number
}
```

### ja-translated `waasConfigKey`

| MOCKDATA + ja-translated Type   | ja-translated Default              |
| ------------------------------- | ---------------------------------- |
| MOCKDATA + ja-translated string | MOCKDATA + ja-translated undefined |

MOCKDATA + ja-translated The Embedded Wallet configuration key required for WaaS wallets, configured within the [Sequence Builder](https://sequence.build).

## ja-translated Network

### MOCKDATA + ja-translated `chainIds`

| MOCKDATA + ja-translated Type | ja-translated Default              |
| ----------------------------- | ---------------------------------- |
| ja-translated number[]        | MOCKDATA + ja-translated undefined |

MOCKDATA + ja-translated A list of chain Ids. e.g. \[1, 137]

### ja-translated `defaultChainId`

| MOCKDATA + ja-translated Type   | ja-translated Default              |
| ------------------------------- | ---------------------------------- |
| MOCKDATA + ja-translated number | MOCKDATA + ja-translated undefined |

MOCKDATA + ja-translated The chain Id that is first used for signatures and transactions.

## ja-translated UI

MOCKDATA + ja-translated User Interface based parameters that augment the modal interface.

### ja-translated Sign In Modal Configuration (`signIn`)

MOCKDATA + ja-translated The `signIn` object is used to configure the sign in modal.

#### ja-translated `signIn.logoUrl`

<div>
  <img src="/img/kit/sign-in-modal-with-logo.png" alt="Enable a dark theme to Sequence Kit" />
</div>

| MOCKDATA + ja-translated Type   | ja-translated Default              |
| ------------------------------- | ---------------------------------- |
| MOCKDATA + ja-translated string | MOCKDATA + ja-translated undefined |

MOCKDATA + ja-translated URL of the logo to be shown in the sign in modal.

#### ja-translated `signIn.projectName`

<div>
  <img src="/img/kit/sign-in-modal-project-name.png" alt="Add a project name to Sequence Kit" />
</div>

| MOCKDATA + ja-translated Type   | ja-translated Default              |
| ------------------------------- | ---------------------------------- |
| MOCKDATA + ja-translated string | MOCKDATA + ja-translated undefined |

MOCKDATA + ja-translated Name of the project to be shown in the sign in modal.

#### ja-translated `signIn.useMock`

| MOCKDATA + ja-translated Type    | ja-translated Default              |
| -------------------------------- | ---------------------------------- |
| MOCKDATA + ja-translated boolean | MOCKDATA + ja-translated undefined |

MOCKDATA + ja-translated Removes the ability to make live blockchain requests if set to `true` by using the [wagmi mock connector](https://wagmi.sh/core/api/connectors/mock#mock)

### ja-translated `position`

| MOCKDATA + ja-translated Type   | ja-translated Default           |
| ------------------------------- | ------------------------------- |
| MOCKDATA + ja-translated string | MOCKDATA + ja-translated center |

ja-translated The position parameter determines the location of the various modals on the screen. Possible values include:

- MOCKDATA + ja-translated center
- MOCKDATA + ja-translated middle-right
- ja-translated middle-left
- MOCKDATA + ja-translated top-center
- ja-translated top-right
- MOCKDATA + ja-translated top-left
- ja-translated bottom-center
- MOCKDATA + ja-translated bottom-right
- ja-translated bottom-left

### MOCKDATA + ja-translated `defaultTheme`

| MOCKDATA + ja-translated Type  | ja-translated Default         |
| ------------------------------ | ----------------------------- |
| ja-translated string or object | MOCKDATA + ja-translated dark |

ja-translated The defaultTheme determines the color palette used for styling the modal. Possible values include:

- MOCKDATA + ja-translated 'light'
- ja-translated 'dark'
- MOCKDATA + ja-translated object

MOCKDATA + ja-translated Specific colors can be overwritten by passing a theme override object. The [Sequence Builder](https://sequence.build/) provides a useful playground for toying with the colors in Sequence Kit.

## ja-translated Wallet

ja-translated Parameters that entail wallet configuration options

### MOCKDATA + ja-translated `disableAnalytics`

| MOCKDATA + ja-translated Type    | ja-translated Default              |
| -------------------------------- | ---------------------------------- |
| MOCKDATA + ja-translated boolean | MOCKDATA + ja-translated undefined |

ja-translated Turning on and off the analytics feature that is connected to your [Sequence Builder](http://sequence.build) project.

### MOCKDATA + ja-translated `displayedAssets`

| MOCKDATA + ja-translated Type                                       | ja-translated Default              |
| ------------------------------------------------------------------- | ---------------------------------- |
| ja-translated `[{ contractAddress: string, chainId: number }, ...]` | MOCKDATA + ja-translated undefined |

MOCKDATA + ja-translated If provided, this will determine which assets are to be displayed in the in-game wallet modal main view.
By passing a list of displayed assets, only assets from the provided list will be displayed in the main view.
In the case that no assets are provided, all owned assets can be displayed in the main view.

### ja-translated `enableConfirmationModal`

| MOCKDATA + ja-translated Type    | ja-translated Default              |
| -------------------------------- | ---------------------------------- |
| MOCKDATA + ja-translated boolean | MOCKDATA + ja-translated undefined |

MOCKDATA + ja-translated Enable confirmations for when sending transaction

## ja-translated Sign in providers

MOCKDATA + ja-translated The various sign in providers that create wallet connections for the user:

### ja-translated `walletConnect`

| MOCKDATA + ja-translated Type                               | ja-translated Default     |     |
| ----------------------------------------------------------- | ------------------------- | --- |
| MOCKDATA + ja-translated `false` \| `{ projectId: string }` | ja-translated `undefined` |

### MOCKDATA + ja-translated `google`

| MOCKDATA + ja-translated Type                   | ja-translated Default     |     |
| ----------------------------------------------- | ------------------------- | --- |
| ja-translated `false` \| `{ clientId: string }` | ja-translated `undefined` |

### MOCKDATA + ja-translated `apple`

| MOCKDATA + ja-translated Type                                        | ja-translated Default     |     |
| -------------------------------------------------------------------- | ------------------------- | --- |
| ja-translated `false` \| `{ clientId: string, redirectURI: string }` | ja-translated `undefined` |

### MOCKDATA + ja-translated `email`

| MOCKDATA + ja-translated Type                             | ja-translated Default     |     |
| --------------------------------------------------------- | ------------------------- | --- |
| ja-translated `boolean` \| `{ legacyEmailAuth: boolean }` | ja-translated `undefined` |

# MOCKDATA + ja-translated Create Universal Default Connectors

ja-translated While we generally recommed using Embedded Wallets with SequenceKit, as an alternative, you can also use leverage our Universal Wallet configuration. When creating a wagmi `connectors` variable, import the `getDefaultConnectors` function from the `@0xsequence/kit` package, and include a Wallet Connect ID obtained from [here](https://cloud.walletconnect.com/app), a default chain ID, app name, and the `projectAccessKey`, then [continue with the integration from the quickstart](/solutions/wallets/sequence-kit/getting-started#create-wagmi-config).

```typescript [config.ts]
import { getDefaultConnectors } from '@0xsequence/kit'
...
export const projectAccessKey = '<access-key>'

const connectors = getDefaultConnectors( "universal", {
  walletConnectProjectId: 'wallet-connect-id',
  defaultChainId: 1,
  appName: 'demo app',
  projectAccessKey
})

export const config = createConfig({
  transports,
  connectors,
  chains
})
```
