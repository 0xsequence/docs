---
title: Descripción general del Indexer
description: Saldos en tiempo real, transferencias, NFTs, precios y eventos de contratos en cadenas EVM con webhooks y suscripciones. Úselo como su capa de lectura en producción para aplicaciones onchain.
sidebarTitle: Descripción general
---

**Indexer** le ofrece lecturas de baja latencia en cadenas EVM: saldos y portafolio, propiedad de tokens y NFTs, transferencias y logs, precios y eventos de contratos. Está diseñado para producción: paginación basada en cursor, filtros, webhooks y suscripciones a eventos. Todo en un servicio escalable con 99.99% de disponibilidad.

<CardGroup>
  <Card title="Referencia de la API" href="/api-references/indexer/overview">
    Endpoints, filtros, paginación y formatos de respuesta.
  </Card>

  <Card title="Saldos de tokens cross-chain" href="/solutions/indexer/cross-chain-balances">
    Obtenga saldos de tokens en múltiples cadenas EVM con una sola llamada a la API.
  </Card>

  <Card title="Saldos de tokens" href="/solutions/indexer/token-balances">
    Obtenga saldos de tokens para cualquier wallet o dirección de contrato.
  </Card>

  <Card title="Historial de transacciones" href="/solutions/indexer/transaction-history">
    Obtenga el historial de transacciones para cualquier wallet o dirección de contrato.
  </Card>

  <Card title="Suscripciones" href="/solutions/indexer/subscriptions">
    Suscríbase a eventos, recibos y actualizaciones de saldo para cualquier wallet o dirección de contrato.
  </Card>

  <Card title="Webhooks" href="/solutions/indexer/webhooks">
    Reciba notificaciones webhook para cualquier evento emitido por un contrato.
  </Card>
</CardGroup>

## Qué puede construir

- **Vistas de wallet y portafolio**: saldos y posiciones unificadas en todas las cadenas.
- Feeds de actividad: transferencias, minteos, quemas, ventas.
- **Exploradores de colecciones**: NFTs con metadatos y propiedad.
- **Analítica**: precios, volúmenes, holders y eventos de contratos.

## Inicio rápido
Utilice los SDKs para el camino más rápido, o llame directamente a la API REST. Los ejemplos a continuación ilustran solicitudes típicas; consulte la referencia de la API para los parámetros y campos de respuesta exactos.

<CodeGroup>
  ```shell Curl
  curl -X POST https://polygon-indexer.sequence.app/rpc/Indexer/GetTokenBalances \
   -H "Content-Type: application/json" \
   -H "X-Access-Key: AQAAAAAAAF_JvPALhBthL7VGn6jV0YDqaFY" \
   -d '{ "accountAddress": "0x8e3E38fe7367dd3b52D1e281E4e8400447C8d8B9", "includeMetadata": true }'
  ```

  ```ts Typescript
  // Make sure to install the @0xsequence/indexer package:
  // npm install @0xsequence/indexer
  // or
  // yarn add @0xsequence/indexer

  import { SequenceIndexer } from '@0xsequence/indexer'

  const indexer = new SequenceIndexer('https://polygon-indexer.sequence.app', 'AQAAAAAAAF_JvPALhBthL7VGn6jV0YDqaFY')

  // try any account address you'd like :)
  const accountAddress = '0xabc...'

  // query Sequence Indexer for all token balances of the account on Polygon
  const tokenBalances = await indexer.getTokenBalances({
  	accountAddress: accountAddress,
  	includeMetadata: true
  })
  console.log('tokens in your account:', tokenBalances)
  ```

  ```go Go
  import (
  	"context"
  	"fmt"
  	"log"
  	"net/http"

  	"github.com/0xsequence/go-sequence/indexer"
  )

  func GetTokenBalances(accountAddress string) {
  	seqIndexer := indexer.NewIndexer("https://polygon-indexer.sequence.app", "AQAAAAAAAF_JvPALhBthL7VGn6jV0YDqaFY")

  	includeMetadata := true

  	tokenBalances, _, err := seqIndexer.GetTokenBalances(context.Background(), &accountAddress, nil, &includeMetadata, nil, nil)
  	if err != nil {
  		log.Fatal(err)
  	}
  	fmt.Println("tokenBalances:", tokenBalances)
  }
  ```
</CodeGroup>

## Redes compatibles
Puede ver la [lista completa de redes compatibles aquí](https://status.sequence.info).

## Lecturas adicionales
Aquí tiene algunos ejemplos de consultas que puede hacer a una blockchain desde su aplicación, juego o wallet:
- [Obtenga todos los tokens y NFTs en un wallet a través de todas las EVMs](/solutions/indexer/cross-chain-balances)
- [Obtenga todos los tokens y NFTs en cualquier wallet, incluyendo todos los metadatos](/solutions/indexer/token-balances)
- [Obtenga el historial de transacciones para cualquier dirección de wallet](/solutions/indexer/transaction-history)
- [Obtenga todos los tokens únicos en un contrato ERC20/721/1155 en particular, incluyendo suministros totales](/solutions/indexer/token-balances)
- [Obtenga el historial de transacciones para cualquier dirección de contrato de token](/solutions/indexer/transaction-history)
- [Escuche transacciones de tokens/contratos/direcciones específicas mediante webhooks](/solutions/indexer/webhooks)
- [Suscríbase a eventos, recibos y actualizaciones de saldo para cualquier wallet o dirección de contrato](/solutions/indexer/subscriptions)