---
title: useTransferModal
description: Hook that provides functions to transfer NFTs from your account to another account
sidebarTitle: useTransferModal
---

## Return

An object containing the `show` and `close` functions to initiate NFT transfers from your account to another account

## Import

```typescript
import { useTransferModal } from "@0xsequence/marketplace-sdk/react";
```

## Example

Basic usage:

```typescript
const { show: showTransferModal } = useTransferModal();

return (
  <button
    onClick={() => {
      showTransferModal({
        collectionAddress: collectionAddress,
        chainId: collectionChainId,
        collectibleId: tokenId,
      });
    }}
  >
    Transfer
  </button>
);
```