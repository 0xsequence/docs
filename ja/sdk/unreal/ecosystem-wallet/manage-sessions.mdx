---
title: セッションの管理
description: エコシステムウォレット ドキュメント for SequenceのUnreal SDK
---

### セッションを追加する
既存のログイン方法に追加のセッションを追加できます。すべてのセッションは同じウォレットアドレスを共有しますが、各セッションごとに異なる権限を割り当てることが可能です。`AddSession` を呼び出す際には必ず `IPermissions` オブジェクトを指定してください。

<Tabs>
  <Tab title="ブループリント">
    <Frame>
      <img src="/images/unreal/ecosystem/add_session.png" />
    </Frame>
  </Tab>

  <Tab title="C++">
    ```cpp
    const TSuccessCallback<bool> SuccessCallback = [OnSuccess](const bool& Result) { };
    const FFailureCallback FailureCallback = [OnFailure](const FSequenceError& Error) { };

    USequenceWallet* Wallet = NewObject<USequenceWallet>();
    Wallet->AddSession(Permissions, SuccessCallback, FailureCallback);
    ```
  </Tab>
</Tabs>

### ウォレットの存在確認
ユーザーがすでにウォレットでサインインしているか確認します。これが false を返した場合、ユーザーにサインインしてもらう必要があります。

<Tabs>
  <Tab title="ブループリント">
    <Frame>
      <img src="/images/unreal/ecosystem/check_signedin.png" />
    </Frame>
  </Tab>

  <Tab title="C++">
    ```cpp
    USequenceWallet* Wallet = NewObject<USequenceWallet>();
    bool Exists = Wallet->CheckIfWalletExists();
    ```
  </Tab>
</Tabs>

### セッションのクリア
すべてのセッションをストレージから削除します。これは「サインアウト」機能などでご利用ください。

<Tabs>
  <Tab title="ブループリント">
    <Frame>
      <img src="/images/unreal/ecosystem/clear_sessions.png" />
    </Frame>
  </Tab>

  <Tab title="C++">
    ```cpp
    USequenceWallet* Wallet = NewObject<USequenceWallet>();
    Wallet->ClearSessions();
    ```
  </Tab>
</Tabs>

### ウォレット情報の取得
現在のウォレットから関連情報を取得します。

<Tabs>
  <Tab title="ブループリント">
    <Frame>
      <img src="/images/unreal/ecosystem/wallet_info.png" />
    </Frame>
  </Tab>

  <Tab title="C++">
    ```cpp
    USequenceWallet* Wallet = NewObject<USequenceWallet>();
    FWalletInfo WalletIno = Wallet->GetWalletInfo();
    ```
  </Tab>
</Tabs>