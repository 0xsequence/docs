---
title: useInventory
description: 特定コレクションのユーザーコレクティブルインベントリを無限スクロールで取得 このフックは、マーケットプレイスAPIとインデクサーの両方からデータを組み合わせて、所有しているコレクティブルを完全に表示します。LAOS ERC-721コレクションも自動的に検出し、適切に処理します。マーケットプレイスに出品されていないトークンも含め、すべての所有トークンが返されることを保証します。
sidebarTitle: useInventory
category: Data Fetching
---

## パラメータ

| 名前     | 型 | 説明                                         |
| ------ | - | ------------------------------------------ |
| `args` |   | インベントリ取得のための設定                             |
| `args` |   | .accountAddress - インベントリを取得するウォレットアドレス     |
| `args` |   | .collectionAddress - コレクションのコントラクトアドレス     |
| `args` |   | .chainId - ブロックチェーンネットワークID                |
| `args` |   | .query - オプションのクエリ設定                       |
| `args` |   | .query.enabled - クエリを有効にするかどうか（デフォルト：true） |

## 戻り値
returns.isFetchingNextPage - 次のページを取得中は true です

## 例
ページネーションを利用した基本的な使い方：

```typescript
const {
data,
fetchNextPage,
hasNextPage,
isLoading
} = useInventory({
accountAddress: '0x...',
collectionAddress: '0x...',
chainId: 137
});
const allCollectibles = data?.pages.flatMap(page => page.collectibles) ?? [];
return (
<div>
{allCollectibles.map(item => (
<CollectibleCard
key={item.metadata.tokenId}
metadata={item.metadata}
balance={item.balance}
/>
))}
{hasNextPage && (
<button onClick={() => fetchNextPage()}>
Load More
</button>
)}
</div>
);
```

フィルタリングや残高表示を含めた使い方：

```typescript
const { data } = useInventory({
accountAddress: userAddress,
collectionAddress: collection.address,
chainId: collection.chainId,
query: {
enabled: !!userAddress && isConnected
}
});
// Get all items across pages
const inventory = data?.pages.flatMap(p => p.collectibles) ?? [];
// Show ERC1155 balances
inventory.forEach(item => {
if (item.contractType === ContractType.ERC1155) {
console.log(`Token ${item.metadata.tokenId}: ${item.balance} owned`);
}
});
```

## 基本的な使い方

```typescript
import { useInventory } from '@0xsequence/marketplace-sdk/react/hooks';

const result = useInventory({
  // Add your parameters here
});
```